# Core
numpy>=1.24
fastapi>=0.110
uvicorn>=0.29

# Embedding model (recommended for real semantic search).
# If unavailable, the app falls back to a built-in offline hashing embedder.
sentence-transformers>=2.2

# AI answers (RAG) with Claude — needs ANTHROPIC_API_KEY at runtime
anthropic>=1.5

# Testing
pytest>=7.0
httpx>=0.27
